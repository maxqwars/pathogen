import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maxqwars.pathogen',
  appName: 'Pathogen',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
